# Weather

Live weather and short forecasts.

Tool hints: Prefer `get_weather`; use `search_web` only as fallback for official alerts or missing details.

This bundle is present for Skills v2 discovery and compact-catalog loading in Phase 1.
Live runtime prompt ownership stays in the legacy manifest path for now.
